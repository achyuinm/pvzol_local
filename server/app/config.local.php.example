<?php
/**
 * Local-only config (do not commit real secrets).
 *
 * Copy this file to `config.local.php` and edit values.
 * The real `config.local.php` is ignored by git.
 */

return [
    'db' => [
        'driver' => 'mysql',
        'host' => '127.0.0.1',
        'port' => 3306,
        'name' => 'pvzol_root',
        'user' => 'root',
        'pass' => 'CHANGE_ME',
        'charset' => 'utf8mb4',
    ],
];

